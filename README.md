# Productive

A project management app clone built with Next.js 16, Prisma 7, and SQLite.

## Setup

```bash
npm install
npm run db:push
npm run db:seed
npm run dev
```

Login via OTP sent to your email (or printed to console in dev mode).
